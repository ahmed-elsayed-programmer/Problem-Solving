class Solution {
  public boolean isPalindrome(int x) {
      if(x < 0) return false ;
      StringBuilder rev = new StringBuilder(x + "").reverse(); 
      long revNumber  =  Long.valueOf(rev.toString()) ;
      
      return (x == revNumber) ;

  }
}